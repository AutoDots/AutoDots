<!DOCTYPE html>
<html>
<head>
    <title>Braille Chat</title>
    <link rel="stylesheet" href="/styles/app.css">
    <!--  Add some basic styling (optional) -->
    <style>
        body { font-family: sans-serif; }
        textarea { width: 80%; height: 150px; margin-bottom: 10px; }
        pre { border: 1px solid #ccc; padding: 10px; background-color: #f0f0f0; }
        .error { color: red; }
    </style>
</head>
<body>
    <h1>Braille Translator</h1>

    <form method="post" action="/chat/post"> <!-- Corrected action URL -->
        <label for="text">Enter text or LaTeX:</label><br>
        <textarea id="text" name="text" rows="4" cols="50">#if(inputText){#(inputText)}</textarea><br><br>
        <button type="submit">Translate</button>
    </form>

    #if(output):
        <h2>Braille Output:</h2>
        <pre>#(output)</pre>
    #endif

    #if(errorMessage):
        <p class="error">Error: #(errorMessage)</p>
    #endif
</body>
</html>